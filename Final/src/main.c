#include <stdio.h>
#include "five.h"

int main(){
	int i; 
	ini(); 
	clientNum = 1; 
	chess(1, 10, 10);
	chess(2, 11, 11); 
	chess(1, 12, 12); 
	chess(2, 11, 12);
	for(i=0;i<50;i++)
		chat(i+1, "Hello World!!");
	show(); 
	return 0; 
}
